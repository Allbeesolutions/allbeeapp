# Handoff — AllBee app

Read `AGENTS.md`, `HAO_POLICY.json`, `CURRENT_STATE.md`, `WORK_QUEUE.md`, `APN_SCOPE_PROFILE.md`, `FINANCE_RPC_DESIGN.md`, `RELEASE_CHECKLIST.md`, and `BLOCKERS.md`. HHC is the primary executor. The first two local commits are `83104b3` and `6bde213`; both reviewed against current changes. This session adds tab-specific APN scopes and role-scoped bootstrap, a stable tab refresh/realtime path, failure feedback in income/expense form, partial-save/retry tests, and mocked role viewport checks. Full suite 217/217 and build pass. No production changes. Do not certify live DB, custom domain or signed-in production roles from local mocks. Pending external release gates are in the checklist; keep HAO active for local work without treating those gates as a global block.
