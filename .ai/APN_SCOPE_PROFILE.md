# APN scope and local performance profile — 2026-09-26

`routeDataTables(route, role, tab)` maps every partner and admin tab. The shared shell includes notification/action badge sources so counts survive a direct link or refresh. Partner scopes range from 6 to 14 tables (home 14, learn 8, network 9, wallet 10, withdrawals 13); admin scopes range from 13 to 24 (partners 21, docs 13, withdrawals 21, referrals 24). The previous generic APN route declared about 80 tables. Component-owned chat, support, network, legal-status and finance-snapshot RPCs remain independent.

Mocked Chromium at 320, 360, 375, 390, 430, 768, 1024 and 1440 px: admin APN, staff dashboard, client portal and partner home all rendered with zero document horizontal overflow and zero page errors (32 checks). Isolated Supabase mock, no production credentials or real writes. At 390 px, instrumented successful table requests: admin APN 23, staff 15, client 10, partner home 20. Earlier local mock before role-specific bootstrap recorded 27/15/15/27 respectively. Request counts include profile/config reads; they are local mock measurements, not live egress or latency. The admin/partner shell preserves badge facts, so tab-specific scopes still perform some shared reads.

Navigation/refresh/realtime integration tests: partner Learn and Network tabs, scoped manual refresh, admin Materials tab and its INSERT event. The fetch generation/route guard now avoids unrelated collection replacement. Partner financial snapshot is requested only for home, wallet, withdrawals, profile and district; heavy partner summary calculation runs only on views that use it.

Vite build: main index 511.42 kB (146.51 kB gzip), APNAdmin 90.09 kB (24.28 kB gzip), jsPDF 341.61 kB (110.91 kB gzip, separately lazy chunk). The main chunk still triggers Vite's 500 kB warning. This work targeted measured data requests; further JS extraction should be benchmarked separately to avoid destabilizing the central app. Real network egress and RLS behavior need authenticated staging/production sessions.
